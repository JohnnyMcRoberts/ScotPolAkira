<UserControl x:Class="ScotPolWpfApp.Views.PredictionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScotPolWpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label>Last Updated:</Label>
            <Label Grid.Column="1" Content="{Binding LastUpdated}" ContentStringFormat="dd MMM yyyy hh:mm:ss"/>
        </Grid>

        <TabControl Grid.Row="1">
            <TabItem Header="Poll Details">

            </TabItem>
            <TabItem Header="Trends By Time">

            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
